# Direct dependencies only. See pyproject.toml for the authoritative list.
#
# CPU install:
#     uv sync
# CUDA install (adjust the wheel index to match your driver):
#     uv pip install torch --index-url https://download.pytorch.org/whl/cu126
matplotlib>=3.10
numba>=0.61
numpy>=2.0
pandas>=2.2
scikit-learn>=1.5
scipy>=1.14
torch>=2.4
tqdm>=4.66
